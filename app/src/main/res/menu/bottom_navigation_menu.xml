<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/calendarFragment"
        android:title="@string/calendar_fragment_title"
        android:icon="@drawable/ic_navigation_calendar"
        app:showAsAction="ifRoom">
    </item>

    <item android:id="@+id/dashboardFragment"
        android:title="@string/dashboard_fragment_title"
        android:icon="@drawable/ic_navigation_dashboard"
        app:showAsAction="ifRoom">
    </item>

    <item android:id="@+id/relaxFragment"
        android:title="@string/relax_fragment_title"
        android:icon="@drawable/ic_navigation_relax"
        app:showAsAction="ifRoom">
    </item>

    <item android:id="@+id/settingsFragment"
        android:title="@string/settings_fragment_title"
        android:icon="@drawable/ic_navigation_settings"
        app:showAsAction="ifRoom">
    </item>

</menu>